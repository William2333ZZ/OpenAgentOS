#include "elfload.h"
#include "../include/agentos.h"

int agent_load_from_path(const char *path, const char *name, unsigned int caps) {
    (void)path;
    (void)name;
    (void)caps;
    return EINVAL;
}
