# OpenAgentOS 0.6.4 — 设计草案

状态：**规划中**  
基线：**0.6.3**  
主题：**Sandbox 统一视图**  
验收：`make check-0.6.4`  
索引：[SANDBOX.md](./SANDBOX.md)

## 交付

| 项 | 说明 |
|----|------|
| Console `/sandbox status` | 汇总 cap / namespace / quota / policy / net_allow |
| Console `/sandbox probe <tool-id>` | 调用 policy probe + 打印结果 |
| Demo | init 脚本验证 isolation + net 成功路径 |

## 输出示例

```
[sandbox] agent=10 caps=0x... ipc=2/16 tool=5/128 fs=1/32
[sandbox] ns=/ns/agent/10/home policy=loaded net_allow=2
```

## 意义

0.6.x **minor 线收官**：对外可演示「Agent Sandbox Runtime」完整故事，再进入 **0.7.0 安全 minor**。
